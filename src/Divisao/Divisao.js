import '../App'

function Divisao(props){
    return(
        <div>
            <h1>Divisao de {props.a} / {props.b} é igual a: {props.a / props.b}</h1>
        </div>
    )
}

export default Divisao;